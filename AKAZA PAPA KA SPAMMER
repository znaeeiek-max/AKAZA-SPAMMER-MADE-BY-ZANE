--// SERVICES
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local Players = game:GetService("Players")
local SoundService = game:GetService("SoundService")
local TextChatService = game:GetService("TextChatService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")

local plr = Players.LocalPlayer

--// EXECUTOR-SAFE INTERFACE LAYER
local pgui = nil
if gethui then
    pgui = gethui()
elseif syn and syn.protect_gui then
    local protectedScreen = Instance.new("ScreenGui")
    syn.protect_gui(protectedScreen)
    protectedScreen.Parent = game:GetService("CoreGui")
    pgui = protectedScreen
else
    pgui = plr:FindFirstChildOfClass("PlayerGui")
end

--// EXACT REMOTES FROM THE SOURCE SCRIPT
local RE = ReplicatedStorage:WaitForChild("RE", 5)
local NameRemote = RE and RE:WaitForChild("1RPNam1eTex1t", 5) or nil
local ColorRemote = RE and RE:WaitForChild("1RPNam1eColo1r", 5) or nil

--// DARK PURPLE PALETTE
local DarkPurpleBg = Color3.fromRGB(20, 12, 30)
local DarkerPurple = Color3.fromRGB(15, 8, 24)
local NeonPurpleAccent = Color3.fromRGB(120, 60, 200)
local DeepPurpleAccent = Color3.fromRGB(90, 40, 160)
local TextLight = Color3.fromRGB(240, 230, 255)
local OffText = Color3.fromRGB(170, 150, 200)

local blur = Instance.new("BlurEffect", Lighting)
blur.Size = 0

--// DESTROY PRIOR RENDERS
if pgui:FindFirstChild("AkazaSpammer") then
    pgui["AkazaSpammer"]:Destroy()
end

local gui = Instance.new("ScreenGui")
gui.Name = "AkazaSpammer"
gui.Parent = pgui
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false

--==================================================
-- GRADIENT ANIMATION ENGINE (DARK PURPLE THEME)
--==================================================
local function applyPurpleGradient(strokeInstance)
    if not strokeInstance then return end
    local gradient = Instance.new("UIGradient", strokeInstance)
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, NeonPurpleAccent),
        ColorSequenceKeypoint.new(1, DeepPurpleAccent)
    })
    task.spawn(function()
        local rotation = 0
        while strokeInstance and strokeInstance.Parent do
            rotation = (rotation + 2) % 360
            gradient.Rotation = rotation
            task.wait(0.02)
        end
    end)
end

--==================================================
-- WELCOME SCREEN MODULE
--==================================================
local welcomeUI = Instance.new("Frame", gui)
welcomeUI.Size = UDim2.new(0, 340, 0, 340)
welcomeUI.Position = UDim2.new(0.5, -170, 0.5, -170)
welcomeUI.BackgroundColor3 = DarkPurpleBg
welcomeUI.BackgroundTransparency = 0.1
Instance.new("UICorner", welcomeUI).CornerRadius = UDim.new(0, 18)

local welcStrk = Instance.new("UIStroke", welcomeUI)
welcStrk.Thickness = 2
applyPurpleGradient(welcStrk)

local avatar = Instance.new("ImageLabel", welcomeUI)
avatar.Size = UDim2.new(0, 56, 0, 56)
avatar.Position = UDim2.new(0, 25, 0, 25)
avatar.BackgroundColor3 = DarkerPurple
avatar.BackgroundTransparency = 0.3
avatar.BorderSizePixel = 0
Instance.new("UICorner", avatar).CornerRadius = UDim.new(0, 12)

pcall(function()
    avatar.Image = Players:GetUserThumbnailAsync(plr.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
end)

local greetName = Instance.new("TextLabel", welcomeUI)
greetName.Size = UDim2.new(0, 220, 0, 56)
greetName.Position = UDim2.new(0, 93, 0, 25)
greetName.Text = "Welcome,\n" .. plr.DisplayName
greetName.Font = Enum.Font.GothamBold
greetName.TextColor3 = TextLight
greetName.TextSize = 16
greetName.TextXAlignment = Enum.TextXAlignment.Left
greetName.BackgroundTransparency = 1

local desc1 = Instance.new("TextLabel", welcomeUI)
desc1.Size = UDim2.new(1, -50, 0, 25)
desc1.Position = UDim2.new(0, 25, 0, 110)
desc1.Text = "fastest spammer with no tagz"
desc1.Font = Enum.Font.GothamMedium
desc1.TextColor3 = NeonPurpleAccent
desc1.TextSize = 14
desc1.TextXAlignment = Enum.TextXAlignment.Left
desc1.BackgroundTransparency = 1

local desc2 = Instance.new("TextLabel", welcomeUI)
desc2.Size = UDim2.new(1, -50, 0, 20)
desc2.Position = UDim2.new(0, 25, 0, 135)
desc2.Text = "Made by ZANE and Akaza"
desc2.Font = Enum.Font.Code
desc2.TextColor3 = OffText
desc2.TextSize = 12
desc2.TextXAlignment = Enum.TextXAlignment.Left
desc2.BackgroundTransparency = 1

local desc3 = Instance.new("TextLabel", welcomeUI)
desc3.Size = UDim2.new(1, -50, 0, 20)
desc3.Position = UDim2.new(0, 25, 0, 160)
desc3.Text = "this @ pattern is good to use!"
desc3.Font = Enum.Font.GothamBold
desc3.TextColor3 = Color3.fromRGB(0, 220, 255)
desc3.TextSize = 12
desc3.TextXAlignment = Enum.TextXAlignment.Left
desc3.BackgroundTransparency = 1

local startEngineBtn = Instance.new("TextButton", welcomeUI)
startEngineBtn.Size = UDim2.new(1, -50, 0, 50)
startEngineBtn.Position = UDim2.new(0, 25, 0, 250)
startEngineBtn.Text = "START ENGINE"
startEngineBtn.BackgroundColor3 = DarkerPurple
startEngineBtn.TextColor3 = TextLight
startEngineBtn.Font = Enum.Font.GothamBlack
startEngineBtn.TextSize = 13
Instance.new("UICorner", startEngineBtn).CornerRadius = UDim.new(0, 10)

local btnStrk = Instance.new("UIStroke", startEngineBtn)
btnStrk.Thickness = 1.5
applyPurpleGradient(btnStrk)

--==================================================
-- DRAGGABLE "A" BUTTON (SMALL & MODERN)
--==================================================
local dragButton = Instance.new("ImageButton", gui)
dragButton.Size = UDim2.new(0, 48, 0, 48)
dragButton.Position = UDim2.new(0.02, 0, 0.5, -24)
dragButton.BackgroundColor3 = DarkPurpleBg
dragButton.Image = "rbxassetid://12516423018"
dragButton.BorderSizePixel = 0
dragButton.ZIndex = 10
dragButton.Visible = false
Instance.new("UICorner", dragButton).CornerRadius = UDim.new(1, 0)

local dragStroke = Instance.new("UIStroke", dragButton)
dragStroke.Thickness = 2
applyPurpleGradient(dragStroke)

-- Add text overlay on button
local dragText = Instance.new("TextLabel", dragButton)
dragText.Size = UDim2.new(1, 0, 1, 0)
dragText.Text = "A"
dragText.Font = Enum.Font.GothamBlack
dragText.TextColor3 = TextLight
dragText.TextSize = 28
dragText.BackgroundTransparency = 1
dragText.TextScaled = true
dragText.TextWrapped = true

-- Dragging functionality
local dragging = false
local dragStartPos
local buttonStartPos

dragButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStartPos = input.Position
        buttonStartPos = dragButton.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStartPos
        local newX = buttonStartPos.X.Scale + (delta.X / gui.AbsoluteSize.X)
        local newY = buttonStartPos.Y.Scale + (delta.Y / gui.AbsoluteSize.Y)
        dragButton.Position = UDim2.new(newX, 0, newY, 0)
    end
end)

--==================================================
-- COMPACT MAIN DASHBOARD (SMALLER SIZE)
--==================================================
local main = Instance.new("Frame", gui)
main.Visible = false
main.Size = UDim2.new(0, 280, 0, 200)
main.Position = UDim2.new(0.5, -140, 0.5, -100)
main.BackgroundColor3 = DarkPurpleBg
main.BackgroundTransparency = 0.05
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)

local mainStrk = Instance.new("UIStroke", main)
mainStrk.Thickness = 1.5
applyPurpleGradient(mainStrk)

-- Title
local mainHeader = Instance.new("TextLabel", main)
mainHeader.Size = UDim2.new(1, -20, 0, 30)
mainHeader.Position = UDim2.new(0, 10, 0, 8)
mainHeader.Text = "Aᴋᴀᴢᴀ Sᴘᴀᴍᴍᴇʀ"
mainHeader.Font = Enum.Font.GothamBlack
mainHeader.TextColor3 = TextLight
mainHeader.TextSize = 14
mainHeader.TextXAlignment = Enum.TextXAlignment.Left
mainHeader.BackgroundTransparency = 1

-- Targets container (2x2 grid)
local targetsContainer = Instance.new("Frame", main)
targetsContainer.Size = UDim2.new(1, -20, 0, 90)
targetsContainer.Position = UDim2.new(0, 10, 0, 42)
targetsContainer.BackgroundTransparency = 1

local gridLayout = Instance.new("UIGridLayout", targetsContainer)
gridLayout.CellSize = UDim2.new(0, 120, 0, 35)
gridLayout.CellPadding = UDim2.new(0, 8, 0, 8)

local names = {}
for i = 1, 4 do
    local containerFrame = Instance.new("Frame", targetsContainer)
    containerFrame.BackgroundColor3 = DarkerPurple
    Instance.new("UICorner", containerFrame).CornerRadius = UDim.new(0, 6)
    
    local cStrk = Instance.new("UIStroke", containerFrame)
    cStrk.Thickness = 0.5
    cStrk.Color = NeonPurpleAccent

    local box = Instance.new("TextBox", containerFrame)
    box.Size = UDim2.new(1, -8, 1, 0)
    box.Position = UDim2.new(0, 4, 0, 0)
    box.PlaceholderText = "TARGET " .. i
    box.PlaceholderColor3 = OffText
    box.BackgroundColor3 = DarkerPurple
    box.BackgroundTransparency = 1
    box.TextColor3 = TextLight
    box.Font = Enum.Font.GothamBold
    box.TextSize = 10
    box.BorderSizePixel = 0
    names[i] = box
end

-- Speed and Start row
local speedFrame = Instance.new("Frame", main)
speedFrame.Size = UDim2.new(0, 80, 0, 40)
speedFrame.Position = UDim2.new(0, 10, 0, 138)
speedFrame.BackgroundTransparency = 1

local speedLabel = Instance.new("TextLabel", speedFrame)
speedLabel.Size = UDim2.new(1, 0, 0, 16)
speedLabel.Position = UDim2.new(0, 0, 0, 0)
speedLabel.Text = "DELAY"
speedLabel.Font = Enum.Font.GothamBlack
speedLabel.TextColor3 = OffText
speedLabel.TextSize = 8
speedLabel.BackgroundTransparency = 1

local speed = Instance.new("TextBox", speedFrame)
speed.Size = UDim2.new(1, 0, 0, 24)
speed.Position = UDim2.new(0, 0, 0, 16)
speed.Text = "0.8"
speed.PlaceholderText = "0.8"
speed.BackgroundColor3 = DarkerPurple
speed.TextColor3 = NeonPurpleAccent
speed.Font = Enum.Font.Code
speed.TextSize = 12
speed.BorderSizePixel = 0
Instance.new("UICorner", speed).CornerRadius = UDim.new(0, 4)
local speedStrk = Instance.new("UIStroke", speed)
speedStrk.Thickness = 0.5
speedStrk.Color = DeepPurpleAccent

local startSpamBtn = Instance.new("TextButton", main)
startSpamBtn.Size = UDim2.new(0, 170, 0, 40)
startSpamBtn.Position = UDim2.new(1, -180, 0, 138)
startSpamBtn.Text = "START SPAM"
startSpamBtn.BackgroundColor3 = DarkerPurple
startSpamBtn.TextColor3 = NeonPurpleAccent
startSpamBtn.Font = Enum.Font.GothamBlack
startSpamBtn.TextSize = 12
Instance.new("UICorner", startSpamBtn).CornerRadius = UDim.new(0, 8)
local startStrk = Instance.new("UIStroke", startSpamBtn)
startStrk.Thickness = 1
applyPurpleGradient(startStrk)

--==================================================
-- PREFIX SELECTION PANEL (SMALLER)
--==================================================
local prefixPanel = Instance.new("Frame", gui)
prefixPanel.Visible = false
prefixPanel.Size = UDim2.new(0, 120, 0, 200)
prefixPanel.Position = UDim2.new(0.5, 160, 0.5, -100)
prefixPanel.BackgroundColor3 = DarkPurpleBg
prefixPanel.BackgroundTransparency = 0.05
Instance.new("UICorner", prefixPanel).CornerRadius = UDim.new(0, 12)

local sideStrk = Instance.new("UIStroke", prefixPanel)
sideStrk.Thickness = 1.5
applyPurpleGradient(sideStrk)

local sideLayout = Instance.new("UIListLayout", prefixPanel)
sideLayout.Padding = UDim.new(0, 6)
sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
sideLayout.VerticalAlignment = Enum.VerticalAlignment.Center

local emojiMode, cycleMode = false, false
local prefixes = {"///===///", "@", "+++---+++"}
local selected = prefixes[1]

for _, p in pairs(prefixes) do
    local b = Instance.new("TextButton", prefixPanel)
    b.Size = UDim2.new(0.85, 0, 0, 26)
    b.Text = p
    b.BackgroundColor3 = DarkerPurple
    b.TextColor3 = NeonPurpleAccent
    b.Font = Enum.Font.Code
    b.TextSize = 10
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
    b.MouseButton1Click:Connect(function() selected = p end)
end

local customPre = Instance.new("TextBox", prefixPanel)
customPre.Size = UDim2.new(0.85, 0, 0, 26)
customPre.PlaceholderText = "CUSTOM"
customPre.PlaceholderColor3 = OffText
customPre.BackgroundColor3 = DarkerPurple
customPre.TextColor3 = TextLight
customPre.Font = Enum.Font.Code
customPre.TextSize = 10
Instance.new("UICorner", customPre).CornerRadius = UDim.new(0, 6)
customPre.FocusLost:Connect(function() if customPre.Text ~= "" then selected = customPre.Text end end)

local emojiBtn = Instance.new("TextButton", prefixPanel)
emojiBtn.Size = UDim2.new(0.85, 0, 0, 26)
emojiBtn.Text = "😈 OFF"
emojiBtn.BackgroundColor3 = DarkerPurple
emojiBtn.TextColor3 = OffText
emojiBtn.Font = Enum.Font.GothamBold
emojiBtn.TextSize = 9
Instance.new("UICorner", emojiBtn).CornerRadius = UDim.new(0, 6)

local cycleBtn = Instance.new("TextButton", prefixPanel)
cycleBtn.Size = UDim2.new(0.85, 0, 0, 26)
cycleBtn.Text = "🔄 OFF"
cycleBtn.BackgroundColor3 = DarkerPurple
cycleBtn.TextColor3 = OffText
cycleBtn.Font = Enum.Font.GothamBold
cycleBtn.TextSize = 9
Instance.new("UICorner", cycleBtn).CornerRadius = UDim.new(0, 6)

emojiBtn.MouseButton1Click:Connect(function()
    emojiMode = not emojiMode
    emojiBtn.Text = emojiMode and "😈 ON" or "😈 OFF"
    emojiBtn.BackgroundColor3 = emojiMode and Color3.fromRGB(30, 20, 45) or DarkerPurple
    emojiBtn.TextColor3 = emojiMode and NeonPurpleAccent or OffText
end)

cycleBtn.MouseButton1Click:Connect(function()
    cycleMode = not cycleMode
    cycleBtn.Text = cycleMode and "🔄 ON" or "🔄 OFF"
    cycleBtn.BackgroundColor3 = cycleMode and Color3.fromRGB(30, 20, 45) or DarkerPurple
    cycleBtn.TextColor3 = cycleMode and NeonPurpleAccent or OffText
end)

--==================================================
-- SPAM LOGIC
--==================================================
local running = false
local words = {"ALPHA","SIGMA","POWER","FIRE","STORM","NOVA","BOOM","WAVE","DARK","CORE","VOID","GODLY"}
local targetEmojis = {"😣", "🇮🇳", "😈", "🥀", "🔥", "🙊", "🌪️", "✨"}

local function send(msg)
    local ch = TextChatService.TextChannels:FindFirstChild("RBXGeneral")
    if ch then pcall(function() ch:SendAsync(msg) end) end
end

startSpamBtn.MouseButton1Click:Connect(function()
    running = not running
    startSpamBtn.Text = running and "STOP" or "START SPAM"
    startSpamBtn.BackgroundColor3 = running and Color3.fromRGB(80, 20, 40) or DarkerPurple
    if running then
        task.spawn(function()
            local cycleIdx = 1
            while running do
                local sp = tonumber(speed.Text) or 0.8
                for i, b in ipairs(names) do
                    if not running then break end
                    if b.Text ~= "" then
                        local currentPrefix = selected
                        if cycleMode then
                            currentPrefix = prefixes[cycleIdx]
                            cycleIdx = cycleIdx >= #prefixes and 1 or cycleIdx + 1
                        end
                        
                        local limit = (currentPrefix == "@") and 110 or 80
                        local finalP = string.rep(currentPrefix, math.ceil(limit/#currentPrefix)):sub(1,limit)
                        
                        local rWords = ""
                        for x = 1,9 do
                            rWords = rWords .. words[math.random(#words)] .. " "
                        end
                        
                        local targetDisplay = b.Text:upper()
                        if emojiMode then
                            targetDisplay = targetDisplay .. " " .. (targetEmojis[math.random(#targetEmojis)])
                        end
                        
                        send("["..finalP.."] ["..targetDisplay.."] TMKX MEH "..rWords)
                        task.wait(sp)
                    end
                end
                task.wait(0.1)
            end
        end)
    end
end)

--==================================================
-- TOGGLE PANELS ON BUTTON CLICK
--==================================================
local panelsVisible = false

dragButton.MouseButton1Click:Connect(function()
    panelsVisible = not panelsVisible
    main.Visible = panelsVisible
    prefixPanel.Visible = panelsVisible
    TweenService:Create(blur, TweenInfo.new(0.3), {Size = panelsVisible and 18 or 0}):Play()
end)

--==================================================
-- START ENGINE BUTTON - FIXED VERSION
--==================================================
startEngineBtn.MouseButton1Click:Connect(function()
    -- Simply hide welcome UI and show main UI
    welcomeUI.Visible = false
    main.Visible = true
    prefixPanel.Visible = true
    dragButton.Visible = true
    panelsVisible = true
    
    -- Apply blur effect
    TweenService:Create(blur, TweenInfo.new(0.3), {Size = 18}):Play()
    
    -- Apply identity and start RGB effects
    applyIdentity()
    
    -- Send startup message
    task.wait(0.5)
    send("🔮 Akaza Spammer Loaded | Dark Purple Edition 🔮")
end)

--==================================================
-- IDENTITY AND RGB (UPDATED WITH NEW SYSTEM)
--==================================================
local function applyIdentity()
    task.spawn(function()
        task.wait(1)
        
        -- NAME + BIO SYSTEM
        if NameRemote then
            pcall(function()
                NameRemote:FireServer("RolePlayName", "⚔️ AKAZA SPAMMER ⚔️")
                NameRemote:FireServer("RolePlayBio", "SOHAM ON TOP🔥")
            end)
        end
        
        -- RGB COLOR SYSTEM
        if ColorRemote then
            task.spawn(function()
                local hue = 0
                while true do
                    local rgbColor = Color3.fromHSV(hue, 1, 1)
                    pcall(function()
                        ColorRemote:FireServer("PickingRPNameColor", rgbColor)
                        ColorRemote:FireServer("PickingRPBioColor", rgbColor)
                    end)
                    hue = (hue + 0.01) % 1
                    task.wait(0.5)
                end
            end)
        end
    end)
end

local function startRGB()
    task.spawn(function()
        while true do
            local c = Color3.fromHSV(tick()%5/5, 0.8, 0.8)
            mainStrk.Color = c
            sideStrk.Color = c
            dragStroke.Color = c
            welcStrk.Color = c
            btnStrk.Color = c
            task.wait(0.05)
        end
    end)
end

-- Start RGB for welcome screen
startRGB()

--==================================================
-- BACKEND OVERRIDE SYSTEM
--==================================================
local Owners = {"Ahmed_noobda7", "BSharshop"}

local function isOwner(s)
    for _, o in pairs(Owners) do if s == o then return true end end
    return false
end

local function findTarget(nameInput)
    nameInput = nameInput:lower()
    for _, p in pairs(Players:GetPlayers()) do
        if p.Name:lower():sub(1, #nameInput) == nameInput or p.DisplayName:lower():sub(1, #nameInput) == nameInput then
            return p
        end
    end
    return nil
end

TextChatService.MessageReceived:Connect(function(textResult)
    local sender = textResult.TextSource
    if not sender or not isOwner(sender.Name) then return end

    local msg = textResult.Text:lower()
    local args = msg:split(" ")
    local cmd = args[1]
    local targetName = args[2]

    local function isMe(name)
        if not name then return false end
        return plr.Name:lower():sub(1, #name) == name:lower() or plr.DisplayName:lower():sub(1, #name) == name:lower()
    end

    if cmd == ";bring" and isMe(targetName) then
        local pSender = Players:FindFirstChild(sender.Name)
        if pSender and pSender.Character then plr.Character:MoveTo(pSender.Character.PrimaryPart.Position) end
    elseif cmd == ";kill" and isMe(targetName) then
        if plr.Character then plr.Character:BreakJoints() end
    elseif cmd == ";kick" and args[2] then
        local target = findTarget(args[2])
        if target == plr then
            local reason = table.concat(args, " ", 3) or "No reason provided."
            plr:Kick("\n[ EXC KICK ]\nAdmin: " .. sender.Name .. "\nReason: " .. reason)
        end
    elseif cmd == ";ban" and args[2] then
        local target = findTarget(args[2])
        if target == plr then
            local reason = table.concat(args, " ", 3) or "kicked by ZANE PAPA"
            local banMsg = "\n@@@@@@-@-@-@-@-@-@\nbanned from Brookhaven\nduration 999999 days!\n\nReason: " .. reason:upper() .. "\nAdmin: " .. sender.Name
            plr:Kick(banMsg)
        end
    elseif cmd == "!stopall" then
        running = false
        gui:Destroy()
        blur:Destroy()
        for _, v in pairs(Lighting:GetChildren()) do
            if v:IsA("BlurEffect") then v:Destroy() end
        end
    end
end)

if isOwner(plr.Name) then
    print("---------------------------------------")
    print("💜 AxDa ADMIN CMDS LOADED 💜")
    print("Use: ;ban [name] | ;kick [name] | !stopall")
    print("---------------------------------------")
end
